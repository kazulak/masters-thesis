# Retained Historical Reference

The final publication tree intentionally keeps this archive minimal.

Only historical material that is still path- or hash-bound by the final research
provenance remains here.

## Retained file

```text
architecture-reset-2026/THESIS_BENCHMARK_MATRIX.md
```

This file is retained byte-for-byte because the final PIMutation-family workload
provenance records its exact path and SHA-256, and the final workload-reconciliation
document refers to it as historical 42-case context.

It is historical context, not the final benchmark campaign.

## Removed development material

Earlier architecture-reset audits, migration ledgers, agent guides, temporary runbooks,
patch files, walkthroughs, and intermediate planning documents were removed from the
current publication tree.

They remain available through the preserved Git history and the original
`kazulak/Masters` repository.

Removing those files from the current tree does not rewrite or delete their history.
